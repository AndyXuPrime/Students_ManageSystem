spring:
  application:
    name: sims-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
  profiles:
    active: dev